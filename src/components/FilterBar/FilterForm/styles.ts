import { Box, IconButton } from '@mui/material'
import styled from 'styled-components'

export const Container = styled(Box)`
  width: 100%;

  display: flex;
  align-items: center;

  gap: 2rem;
`

export const ButtonReturn = styled(IconButton)`
  color: #e2e8f0;
`
